<h1>Games</h1>

<ul>
  <% @games.each do |game| %>
    <li>
      <%= link_to game do %>
        #<%= game.id %> - <%= pluralize(game.players.length, 'player') %>
      <% end %>
    </li>
  <% end %>
</ul>

<%= button_to 'New game', games_path %>
